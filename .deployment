[config]
# Disable Oryx build (we build in CI/CD)
SCM_DO_BUILD_DURING_DEPLOYMENT=false
ENABLE_ORYX_BUILD=false

# Note: Dependencies are still installed automatically by Azure
# when it detects requirements.txt, even with Oryx disabled
